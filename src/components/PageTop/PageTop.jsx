import React from "react";

const PageTop = ({ title }) => {
  return (
    <div className="text-themeColor font-semibold text-[22px] leading-[26px]">
      {title}
    </div>
  );
};

export default PageTop;
